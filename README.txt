
To start the project:
1. clone from git the repository https://github.com/atara1/mapOrientation
2. in the terminal install the packages and dependency using the command - npm i
3. run the command ng serve


In the assignment I use the packages:
mapbox-gl
flex-layout
angular/cdk 